/** Modular payment adapter contract. Configure a provider in the deployment layer; the core stays provider-agnostic. */ export class PaymentAdapter { constructor(name) { this.name = name; } async createCheckout() { throw new Error(`${this.name} does not implement createCheckout`); } async verifyWebhook() { throw new Error(`${this.name} does not implement verifyWebhook`); } async normalizeEvent() { throw new Error(`${this.name} does not implement normalizeEvent`); } } export function createPaymentAdapter(name, implementation) { if (!name || !implementation) throw new Error("payment adapter name and implementation are required"); return Object.assign(new PaymentAdapter(name), implementation); }
